{
  "name": "alfred-emoji-fitzpatrick",
  "version": "1.0.0",
  "description": "",
  "main": "./src/process.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -r build || true && mkdir build && cd build && mkdir dark light medium medium-dark medium-light && cd .. && ts-node ./src/process.ts"
  },
  "author": {
    "name": "Joe Woods"
  },
  "license": "MIT",
  "dependencies": {
    "@types/uuid": "^8.3.0",
    "tslint": "^6.1.3",
    "typescript": "^4.0.2",
    "uuid": "^8.3.0"
  },
  "files": [
    "./bin/*",
    "./lib/*",
    "./vendor/*"
  ],
  "devDependencies": {
    "@types/node": "^14.6.4"
  }
}
